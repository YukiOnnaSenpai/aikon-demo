import { NemzaDirective } from './nemza.directive';

describe('NemzaDirective', () => {
  it('should create an instance', () => {
    const directive = new NemzaDirective();
    expect(directive).toBeTruthy();
  });
});
