import { SukiDirective } from './suki.directive';

describe('SukiDirective', () => {
  it('should create an instance', () => {
    const directive = new SukiDirective();
    expect(directive).toBeTruthy();
  });
});
