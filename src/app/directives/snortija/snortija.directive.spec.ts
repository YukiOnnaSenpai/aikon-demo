import { SnortijaDirective } from './snortija.directive';

describe('SnortijaDirective', () => {
  it('should create an instance', () => {
    const directive = new SnortijaDirective();
    expect(directive).toBeTruthy();
  });
});
